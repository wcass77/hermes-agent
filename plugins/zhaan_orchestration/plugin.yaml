name: zhaan_orchestration
version: "0.3.0"
description: "Profile-specific durable AgentMail ingress and pre-agent email routing, Discord session orchestration, and Shared Updates. Disabled unless explicitly enabled."
author: wcass77
hooks:
  - pre_gateway_dispatch
